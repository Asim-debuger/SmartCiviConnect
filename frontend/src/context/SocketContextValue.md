# SocketContextValue.js

Defines and exports the default React context object used for Socket.IO client distribution.

- **Exports**
  - Default export `SocketContext` — a React context initialized to `null`, consumed by `SocketProvider` and the `useSocket` hook.
