Renders an officer directory listing officers assigned across civic departments.

- **Behavior**: Wraps `RoleDirectory` with `role="Officer"`, title "Officer directory", and subtitle "Officers assigned across civic departments."
