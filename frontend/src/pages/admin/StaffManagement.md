Renders a staff directory listing field workers assigned to tasks.

- **Behavior**: Wraps `RoleDirectory` with `role="Staff"`, title "Staff directory", and subtitle "Field workers who accept and complete assigned tasks."
