This file is a thin wrapper that renders the shared `NotificationInbox` component for citizen notifications.

-   **Inbox Passthrough**: Renders `<NotificationInbox />` to display the user's notifications.
