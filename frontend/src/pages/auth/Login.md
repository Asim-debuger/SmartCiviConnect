This file is a thin wrapper that renders the shared `AuthEntry` component in login mode.

-   **Mode Passthrough**: Renders `<AuthEntry mode="login" />` to display the login form and its associated behavior.
