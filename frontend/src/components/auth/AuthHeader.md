This file defines a reusable header component for authentication-related pages.

-   **Functionality**: It displays a title and a short description in a centered format, typically used at the top of login, registration, or password reset forms.
-   **Props**: It accepts `title` and `description` strings to be displayed.
-   **Layout**: The component uses Tailwind CSS for styling, ensuring consistent typography and spacing for all authentication screens.