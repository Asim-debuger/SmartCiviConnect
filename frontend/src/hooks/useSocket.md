# useSocket.js

Custom hook that provides access to the Socket.IO client instance from React context.

- **Exports**
  - Default export `useSocket()` — returns the socket instance provided by `SocketContextValue`.
