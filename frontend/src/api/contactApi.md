This file exposes a single function for handling submissions from the public contact form.

-   **`submitContact(payload)`**: Sends the contact form data (e.g., name, email, message) to the `/public/contact` endpoint.