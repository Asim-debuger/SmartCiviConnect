This file defines the admin workspace layout wrapper.

- **Functionality**: Composes the shared `AppShell` with the `AdminSidebar` to build the full admin UI.
- **Props**: None.
- **Exports**: Default export `AdminLayout` component.
