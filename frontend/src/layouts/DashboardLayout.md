This file defines a simple dashboard layout wrapper.

- **Functionality**: Renders a `Sidebar` alongside a `children` content area.
- **Props**: `children` – rendered inside the `<main>` element.
- **Layout**: Horizontal flex container with the `Sidebar` on the left and the main content area on the right.
