This file is a re-export alias for `PublicNavbar`.

- **Functionality**: Re-exports the default export from `./PublicNavbar` so that consumers can import `Navbar` and receive the same public navbar component.
- **Exports**: Default export forwarded from `PublicNavbar`.
