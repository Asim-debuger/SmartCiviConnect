This file is a re-export alias for `PublicFooter`.

- **Functionality**: Re-exports the default export from `./PublicFooter` so that consumers can import `Footer` and receive the same public footer component.
- **Exports**: Default export forwarded from `PublicFooter`.
