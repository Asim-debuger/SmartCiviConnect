This file defines the citizen workspace layout wrapper.

- **Functionality**: Composes the shared `AppShell` with the `CitizenSidebar` to build the full citizen UI.
- **Props**: None.
- **Exports**: Default export `CitizenLayout` component.
