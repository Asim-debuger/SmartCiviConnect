This file defines the officer workspace layout wrapper.

- **Functionality**: Composes the shared `AppShell` with the common `Sidebar` component (configured for the `"officer"` role) to build the full officer UI.
- **Props**: None.
- **Exports**: Default export `OfficerLayout` component.
