This file defines the staff workspace layout wrapper.

- **Functionality**: Composes the shared `AppShell` with the common `Sidebar` component (configured for the `"staff"` role) to build the full staff UI.
- **Props**: None.
- **Exports**: Default export `StaffLayout` component.
